{
  "mcpServers": {
    "openalgo": {
      "command": "{{OPENALGO_MCP_WRAPPER}}",
      "args": [
        "{{OPENALGO_API_KEY}}",
        "{{OPENALGO_HOST}}"
      ],
      "toolTimeout": 60,
      "enabledTools": ["*"]
    }
  }
}
