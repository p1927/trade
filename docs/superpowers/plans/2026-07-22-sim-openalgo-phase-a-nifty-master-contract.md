# Phase A — NIFTY Master Contract

**Goal:** `stock_simulator` master contract download builds symtoken from HF parquet for NIFTY index + NFO options at `NSE_REPLAY_DATE`.

**Files:** `master_contract.py`, `openalgo/broker/stock_simulator/database/master_contract_db.py`, tests.

**Exit:** Master Contract UI success; expiry/search validate for NIFTY.

See master plan: `sim_openalgo_parity` — Phase A section.
